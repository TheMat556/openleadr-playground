[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
openleadr = "*"
asyncio = "*"
black = "*"
pylint = "*"
pytest = "*"
ruff = "*"
sphinx = "*"
tzdata = "*"

[dev-packages]

[requires]
python_version = "3.12"

[scripts]
start-client = "sh -c 'echo Starting client... && python -m src.client'"
start-server = "python ./src/util/start_server.py"
init-db = "python -m src.sqlite"
test = "pytest"
